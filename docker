# Используйте официальный образ Apache из Docker Hub
FROM httpd:2.4

# Копируйте свойственные вам конфигурационные файлы, если они необходимы
# COPY ./my-apache.conf /usr/local/apache2/conf/httpd.conf

# Можете скопировать свой веб-контент в контейнер, если это необходимо
# COPY ./my-website /usr/local/apache2/htdocs/

# Этот контейнер будет слушать порт 80
EXPOSE 80

# Необходимо использовать эту команду для запуска Apache в фоновом режиме
CMD ["httpd-foreground"]
